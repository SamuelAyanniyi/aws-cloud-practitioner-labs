# IAM (Identity and Access Management) – Hands-On Lab

## What I Did
* Created IAM users
* Attached policies to enforce least privilege
* Enabled MFA on both root account and IAM user account

## What I Learned
* IAM is global, not region-specific
* Least privilege is the security gold standard
* Access keys should be rotated regularly

## Next Steps
* Practice creating custom JSON policies
* Explore IAM roles for EC2 instances
